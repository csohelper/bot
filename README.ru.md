# CSO Helper

[![🇺🇸 English](https://flagcdn.com/w40/us.png)](README.md)
[![🇷🇺 Русский](https://flagcdn.com/w40/ru.png)](/README.ru.md)

**csohelper** — это Telegram-бот для общежития МТУСИ, который предоставляет полезную справочную информацию: контакты, расписания, услуги, дисконтные карты и многое другое.

## Возможности

* Быстрый доступ к информации об общежитии (адрес, почтовый индекс, заведующий, комендант, жилищный отдел, отдел кадров, деканат, поликлиника, библиотека)
* Список дисконтных карт для популярных сетей
* Расписания работы душевых, кухни, прачечной, стиральных машин
* Меню и расписание кафе
* Информация о текущей учебной неделе
* Список полезных приложений для учёбы
* Каталог услуг, предлагаемых жителями
* Поддержка команд и встроенных запросов на русском языке

## Установка

1. Клонировать репозиторий:

    ```sh
    git clone https://github.com/yourusername/csohelper.git
    cd csohelper
    ```

2. Скопировать `.env.example` в `.env` и указать параметры подключения к базе данных:

    ```sh
    cp .env.example .env
    nano .env
    ```

3. Первый запуск бота

    #### Через Docker
    ```sh
    docker compose up
    ```
    #### Без Docker
    Установка
    ```sh
    pip install poetry
    poetry install
    ```
    Запуск
    ```sh
    poetry run csohelper
    ```

4. Настроить файл `storage/config.yaml` (создаётся автоматически при первом запуске)

5. Повторный запуск бота:
    #### Docker
    ```sh
    docker compose up
    ```
    #### Poetry
    ```sh
    poetry run csohelper
    ```

## Структура проекта

`src/python/` — исходный код бота<br>
`src/res/locale/ru.yaml` — локализация и тексты команд<br>
`src/res/images/` — изображения для карточек и кафе<br>
`storage/config.yaml` — конфигурация приложения<br>
`Dockerfile`, `docker-compose.yml` — контейнеризация и запуск базы данных

## Добавление команд и текстов

Все команды и тексты ответов хранятся в `src/res/locale/ru.yaml`. Чтобы добавить новую команду:

1. Добавить описание в секцию `commands`
2. Добавить текст ответа в секцию `echo_commands`
3. Реализовать обработчик

Лицензия
MIT

Честно говоря, я не уверен, что кто-то вообще это читает